using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Utility.存储
{
    public static class HXML
    {
            //        var __文件流 = File.OpenRead(H路径.获取绝对路径("HTTP模板.xml"));
            //XDocument __文档 = XDocument.Load(__文件流);
            //foreach (var __成员 in __文档.Root.Elements("成员"))
            //{
            //    this.listView1.Items.Add(new ListViewItem(new string[]
            //    {
            //        __成员.Attribute("名称").Value,
            //        __成员.Element("地址").Value, 
            //    }));
            //}
            //__文件流.Close();


            //        var __文档 = XDocument.Load(H路径.获取绝对路径("GPS订购配置.xml")).Root;
            //this.in主SSU域名.Text = __文档.Element("连接SSU").Element("主SSU域名").Value;


    }
}
